{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "BAUMAN_ROADMAP_V2_SCHEDULER_RESULT_V1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "requestId",
    "phaseId",
    "weekStart",
    "capacityMinutes",
    "usedMinutes",
    "remainingMinutes",
    "sessions",
    "deferred",
    "readiness",
    "calendarWriteAllowed",
    "persisted",
    "runtimeWriteAllowed"
  ],
  "properties": {
    "schema": { "const": "BAUMAN_ROADMAP_V2_SCHEDULER_RESULT_V1" },
    "requestId": { "type": "string" },
    "phaseId": { "enum": ["GD0", "GD1", "GD2", "GD3"] },
    "weekStart": { "type": "string", "format": "date" },
    "capacityMinutes": { "type": "integer", "minimum": 1 },
    "usedMinutes": { "type": "integer", "minimum": 0 },
    "remainingMinutes": { "type": "integer", "minimum": 0 },
    "sessions": { "type": "array" },
    "deferred": { "type": "array" },
    "readiness": { "type": "object" },
    "calendarWriteAllowed": { "const": false },
    "persisted": { "const": false },
    "runtimeWriteAllowed": { "const": false }
  }
}
