{
  "name": "bauman-control-service",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node scripts/contract-regression.mjs",
    "dev": "wrangler dev --config wrangler.jsonc",
    "deploy": "wrangler deploy --config wrangler.jsonc",
    "migrate:remote": "wrangler d1 migrations apply bauman-control-db --remote --config wrangler.jsonc"
  },
  "devDependencies": {
    "wrangler": "^4.31.0"
  }
}