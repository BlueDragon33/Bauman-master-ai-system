import fs from 'node:fs';
import path from 'node:path';
const root=path.resolve('subjects/russian');
const ai=fs.readFileSync(path.join(root,'assets','ai-mentor-guard.js'),'utf8');
const direct=fs.readFileSync(path.join(root,'assets','ai-direct-explanation.js'),'utf8');
const core=fs.readFileSync(path.join(root,'assets','core.js'),'utf8');
const runtime=fs.readFileSync(path.join(root,'assets','runtime-optimizer.js'),'utf8');
const css=fs.readFileSync(path.join(root,'assets','runtime-optimizer.css'),'utf8');
const sw=fs.readFileSync(path.join(root,'sw.js'),'utf8');
const html=fs.readFileSync(path.join(root,'index.html'),'utf8');
const manifest=JSON.parse(fs.readFileSync(path.join(root,'manifest.webmanifest'),'utf8'));
const need=(text,token,msg)=>{if(!text.includes(token))throw new Error(msg||`Missing token: ${token}`);};
const forbid=(text,token,msg)=>{if(text.includes(token))throw new Error(msg||`Forbidden token: ${token}`);};
need(ai,'RUSSIAN_AI_MENTOR_CONTEXT_V1');need(ai,'canonicalStateReadOnly:true');need(ai,'aiMayModifyMastery:false');need(ai,'RussianLearningState?.get');need(ai,'RussianLearningFlow?.get');need(ai,'RussianVocabSrs?.get');forbid(ai,'RussianVocabSRS?.context','AI guard references nonexistent vocab SRS API');
need(ai,'russianFirst:true');need(ai,'visualContextFirst:true');need(ai,'translationSemanticAuthority:false');need(ai,'englishBridgeDefault:false');need(ai,'metaLanguageSecondaryOnly:true');
need(direct,'RUSSIAN_AI_DIRECT_EXPLANATION_V1');need(direct,'translationSemanticAuthority:false');need(direct,"const ORDER=['visual_or_scene','russian_definition','russian_context','contrast_or_analogy','optional_meta_help']");
forbid(direct,'meaningVi','AI direct explanation must not read Vietnamese meaning fallback');forbid(direct,'.english','AI direct explanation must not read English fallback');forbid(direct,'displayMeaning','AI direct explanation must not reuse legacy display meaning');
need(ai,'Object.values(learning.reviewQueue||{})','AI context must read canonical object-shaped Review Queue');forbid(ai,'Array.isArray(learning.reviewQueue)','AI context must not treat canonical Review Queue as an array');
forbid(ai,'RussianLearningState?.set','AI guard must not write canonical learning state');forbid(ai,'.addReview','AI guard must not enqueue review by itself');forbid(ai,"status:'mastered'",'AI guard must not write mastery');
need(runtime,'RUSSIAN_RUNTIME_OPTIMIZER_V1');need(runtime,'prepareOfflineCore');need(runtime,'reconcileOfflineCore');need(runtime,'countCached');need(runtime,'{refresh:true}');need(runtime,"navigator.connection?.saveData");need(runtime,"serviceWorker.register('./sw.js'");
need(sw,'OPTIONAL_LARGE');need(sw,'dialogue-bauman-az.json');need(sw,'deep-speaking-bauman.json');need(sw,'speaking-link-index.json');need(sw,'russian-app-shell-v1');
need(sw,'isOptionalLarge');need(sw,'Optional source unavailable offline');need(sw,"req.mode==='navigate'");forbid(sw,"catch(()=>caches.match('./index.html'))",'HTML fallback must not be returned for arbitrary failed asset/data requests');
need(css,'@media(max-width:1080px)');need(css,'@media(max-width:760px)');
need(html,'manifest.webmanifest');need(html,'assets/runtime-optimizer.css');need(html,'assets/ai-mentor-guard.js');need(html,'assets/runtime-optimizer.js');
if(manifest.display!=='standalone'||manifest.scope!=='./')throw new Error('Web manifest must stay standalone and Russian-scoped');
const visual=html.indexOf('assets/visual-vocabulary-runtime.js'), directRuntime=html.indexOf('assets/ai-direct-explanation.js'), coreRuntime=html.indexOf('assets/core.js');
if(!(visual>=0&&directRuntime>visual&&coreRuntime>directRuntime))throw new Error('Turn 20 direct-explanation load order invalid');
const academic=html.indexOf('assets/academic-language.js'), guard=html.indexOf('assets/ai-mentor-guard.js'), optimizer=html.indexOf('assets/runtime-optimizer.js'), ui=html.indexOf('assets/russian-reference-ui.js');
if(!(academic>=0&&guard>academic&&optimizer>guard&&ui>optimizer))throw new Error('L7 runtime load order invalid');
const aiStart=core.indexOf('function aiContext()'), aiEnd=core.indexOf('function mediaForm(',aiStart); const aiSurface=core.slice(aiStart,aiEnd);
need(aiSurface,'RussianAIDirectExplanation','Core AI surface must delegate to direct explanation runtime');forbid(aiSurface,'meaningVi','Legacy Vietnamese AI fallback returned');forbid(aiSurface,'.english','Legacy English AI fallback returned');forbid(aiSurface,'English equivalent','Legacy English equivalent output returned');
console.log('RUSSIAN_AI_RUNTIME_GATE=PASS');
console.log('Checks: Russian-first direct-semantic AI explanation, translation fallback blocked, read-only AI context, canonical object-shaped Review Queue, no AI mastery mutation, scoped service worker, verified Cache Storage readiness, user-initiated full offline refresh, Save-Data guard, JSON-safe offline failures and navigation-only HTML fallback.');
