name: Readable Typography CI

on:
  pull_request:
    branches: [main]
    paths:
      - "assets/css/hub-readable-typography-v1.css"
      - "assets/js/hub-safe-shell.js"
      - "tests/hub-readable-typography-v1-static.mjs"
      - "tests/hub-readable-typography-v1-browser.mjs"
      - "tests/subjects-reference-v1-browser.mjs"
      - "tests/thesis-reference-v1-browser.mjs"
      - "index.html"
      - ".github/workflows/readable-typography-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  readable-ui:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22.13.0"

      - name: Static readability gate
        run: |
          node --check assets/js/hub-safe-shell.js
          node --check tests/hub-readable-typography-v1-static.mjs
          node --check tests/hub-readable-typography-v1-browser.mjs
          node tests/hub-readable-typography-v1-static.mjs

      - name: Install Playwright Chromium
        run: |
          printf '{"private":true,"type":"module"}\n' > package.json
          npm install --no-save --no-audit --no-fund playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Start local runtime
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 > /tmp/bauman-readable-http.log 2>&1 &
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:4173/ >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/bauman-readable-http.log
          exit 1

      - name: Run all-tabs readable UI acceptance
        env:
          BAUMAN_E2E_BASE_URL: http://127.0.0.1:4173/
          BAUMAN_E2E_ARTIFACT_DIR: artifacts/hub-readable-typography-v1
        run: node tests/hub-readable-typography-v1-browser.mjs

      - name: Upload readability screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hub-readable-typography-v1-${{ github.run_id }}
          path: artifacts/hub-readable-typography-v1
          if-no-files-found: warn
          retention-days: 7
